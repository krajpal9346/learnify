# Local development
VITE_API_URL=http://localhost:5000/api/v1

# Production (Vercel Project Settings → Environment Variables):
# VITE_API_URL=https://YOUR-API.onrender.com/api/v1
#
# IMPORTANT: Vite bakes env vars into the client at BUILD time.
# After changing VITE_API_URL on Vercel, you MUST redeploy the frontend.
# Do not commit real production URLs or secrets here.
